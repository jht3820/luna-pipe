
/********************************/
/* SYS 계정으로 다음을 수행한다. */
/*******************************/


/*  데이터 저장용 테이블 스페이스 생성 */
CREATE TABLESPACE TS_LUNA_PIPE_DAT DATAFILE

/* 운영체제에 따라 경로는 변경 */
'테이블스페이스 경로\TS_LUNA_PIPE_DAT.DBF' SIZE 200M AUTOEXTEND ON NEXT 200M MAXSIZE UNLIMITED
LOGGING
ONLINE
PERMANENT
EXTENT MANAGEMENT LOCAL AUTOALLOCATE
BLOCKSIZE 8K;


/* TS_LUNA_PIPE_IDX 인덱스 저장용 테이블 스페이스 생성 */
CREATE TABLESPACE TS_LUNA_PIPE_IDX DATAFILE

/* 운영체제에 따라 경로는 변경 */
'테이블스페이스 경로\TS_LUNA_PIPE_IDX.DBF' SIZE 100M AUTOEXTEND ON NEXT 100M MAXSIZE UNLIMITED
LOGGING
ONLINE
PERMANENT
EXTENT MANAGEMENT LOCAL AUTOALLOCATE
BLOCKSIZE 8K;


/* 스키마 생성 및 권한 부여 */
CREATE USER 계정명 IDENTIFIED BY 비밀번호;
GRANT CONNECT,RESOURCE TO 계정명 IDENTIFIED BY 비밀번호;
GRANT CREATE VIEW TO 계정명;
ALTER USER 계정명 DEFAULT TABLESPACE TS_LUNA_PIPE_DAT;
ALTER USER 계정명 TEMPORARY TABLESPACE TEMP;

GRANT CREATE DATABASE LINK TO 계정명;
GRANT CREATE JOB TO 계정명;
GRANT CREATE VIEW TO 계정명;
GRANT CREATE MATERIALIZED VIEW TO 계정명;
GRANT CREATE PROCEDURE TO 계정명;
GRANT CREATE SEQUENCE TO 계정명;
GRANT CREATE SYNONYM TO 계정명;
GRANT CREATE TABLE TO 계정명;
GRANT CREATE TRIGGER TO 계정명;

/* ORACLE 12C 에서는 아래 테이블 스페이스 사용량 언리미티드로 지정 */
alter user LUNAOPSDPLDB default tablespace TS_LUNA_PIPE_DAT quota unlimited on TS_LUNA_PIPE_DAT;
alter user LUNAOPSDPLDB default tablespace TS_LUNA_PIPE_IDX quota unlimited on TS_LUNA_PIPE_IDX;
